#!/usr/bin/env python

from setuptools import setup, find_packages

setup(name='ileed',
      version='0.0.1',
      description='ileed',
      author='',
      author_email='',
      packages=find_packages(),
      install_requires=[
      ],
    )